{% if admin.hasAccess('show', object) and admin.show|length > 0 and admin.hasRoute('show') %}
    <li>
        <a target="_blank" class="sonata-action-element" href="{{ url(object) }}">
            <i class="fa fa-eye" aria-hidden="true"></i>
            {{ 'link_action_show'|trans({}, 'SonataAdminBundle') }}
        </a>
    </li>
{% endif %}
